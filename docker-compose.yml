services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: zena
      POSTGRES_PASSWORD: zena
      POSTGRES_DB: zena
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
